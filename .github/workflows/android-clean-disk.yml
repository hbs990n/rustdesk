name: Build Android ARM64 (Skip Verify)

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - 'flutter/**'
      - 'libs/**'

env:
  RUST_VERSION: "1.75"
  CARGO_NDK_VERSION: "3.1.2"
  ANDROID_FLUTTER_VERSION: "3.24.5"
  NDK_VERSION: "r27c"
  VERSION: "1.4.4"
  VCPKG_COMMIT_ID: "120deac3062162151622ca4860575a33844ba10b"

jobs:
  generate-bridge:
    uses: ./.github/workflows/bridge.yml

  build-android-arm64:
    name: Build Android ARM64 APK
    needs: [generate-bridge]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    
    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
      
      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: ${{ env.NDK_VERSION }}
          add-to-path: true
      
      - name: Setup vcpkg with cache
        uses: lukka/run-vcpkg@v11
        id: setup-vcpkg
        with:
          vcpkgDirectory: /opt/artifacts/vcpkg
          vcpkgGitCommitId: ${{ env.VCPKG_COMMIT_ID }}
          doNotCache: false
      
      - name: Install required packages
        run: |
          sudo apt-get update
          sudo apt-get install -y \
               clang cmake \
               g++ \
               libclang-dev \
               nasm ninja-build \
               openjdk-17-jdk-headless \
               pkg-config
      
      - name: Install flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version: ${{ env.ANDROID_FLUTTER_VERSION }}
          cache: true
      
      - name: Restore bridge files
        uses: actions/download-artifact@master
        with:
          name: bridge-artifact
          path: ./
      
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.RUST_VERSION }}
          components: "rustfmt"
      
      - name: Set environment variables
        run: |
          echo "ANDROID_NDK_HOME=${{ steps.setup-ndk.outputs.ndk-path }}" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=${{ steps.setup-ndk.outputs.ndk-path }}" >> $GITHUB_ENV
          echo "VCPKG_ROOT=/opt/artifacts/vcpkg" >> $GITHUB_ENV
      
      - name: Install cargo-ndk (跳过验证)
        run: |
          echo "安装 cargo-ndk..."
          cargo install cargo-ndk --version ${{ env.CARGO_NDK_VERSION }} --locked
          echo "✅ cargo-ndk 安装完成"
          # 注意：不验证 cargo ndk --version，因为它会报错但实际可用
      
      - name: Build Android dependencies
        run: |
          echo "构建安卓依赖..."
          chmod +x ./flutter/build_android_deps.sh
          ./flutter/build_android_deps.sh "arm64-v8a"
          echo "✅ 依赖构建完成"
      
      - name: Build Rust library using ndk_arm64.sh
        run: |
          echo "构建Rust库..."
          
          # 添加目标架构
          rustup target add aarch64-linux-android
          
          # 直接使用原始脚本（不验证cargo ndk）
          echo "运行 ndk_arm64.sh..."
          chmod +x ./flutter/ndk_arm64.sh
          ./flutter/ndk_arm64.sh
          
          # 检查结果
          if [ -f "target/aarch64-linux-android/release/liblibrustdesk.so" ]; then
            echo "✅ Rust库构建成功"
            mkdir -p flutter/android/app/src/main/jniLibs/arm64-v8a
            cp target/aarch64-linux-android/release/liblibrustdesk.so \
               flutter/android/app/src/main/jniLibs/arm64-v8a/librustdesk.so
          else
            echo "❌ Rust库未找到"
            # 尝试手动构建
            echo "尝试手动构建..."
            cd libs
            cargo ndk --platform 21 --target aarch64-linux-android build --release --features flutter,hwcodec
            cd ..
            if [ -f "target/aarch64-linux-android/release/liblibrustdesk.so" ]; then
              echo "✅ 手动构建成功"
              mkdir -p flutter/android/app/src/main/jniLibs/arm64-v8a
              cp target/aarch64-linux-android/release/liblibrustdesk.so \
                 flutter/android/app/src/main/jniLibs/arm64-v8a/librustdesk.so
            else
              echo "❌ 所有构建方法都失败"
              exit 1
            fi
          fi
      
      - name: Prepare for Flutter build
        run: |
          echo "准备Flutter构建..."
          
          # 确保jniLibs目录存在
          mkdir -p flutter/android/app/src/main/jniLibs/arm64-v8a
          
          # 复制libc++_shared.so（如果存在）
          CXX_LIB="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/libc++_shared.so"
          if [ -f "$CXX_LIB" ]; then
            cp "$CXX_LIB" flutter/android/app/src/main/jniLibs/arm64-v8a/
            echo "复制 libc++_shared.so"
          fi
      
      - name: Build Flutter APK
        env:
          JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64
        run: |
          export PATH=$JAVA_HOME/bin:$PATH
          
          echo "构建Flutter APK..."
          cd flutter
          
          # 清理并构建
          flutter clean
          flutter pub get
          flutter build apk --release --target-platform android-arm64 --split-per-abi
          
          # 重命名APK
          if [ -f "build/app/outputs/flutter-apk/app-arm64-v8a-release.apk" ]; then
            mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk \
               ../rustdesk-${{ env.VERSION }}-arm64.apk
            echo "✅ APK构建成功: rustdesk-${{ env.VERSION }}-arm64.apk"
          else
            echo "❌ APK未找到"
            echo "可用的文件:"
            find build/app/outputs -name "*.apk" -type f
            exit 1
          fi
      
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-android-arm64
          path: rustdesk-${{ env.VERSION }}-arm64.apk
          retention-days: 7
      
      - name: Final success check
        run: |
          echo "========================================"
          echo "✅ 工作流执行完成"
          echo "📱 APK文件: rustdesk-${{ env.VERSION }}-arm64.apk"
          echo "🏗️  状态: 如果看到此消息，APK应该在Artifacts中"
          echo "========================================"
